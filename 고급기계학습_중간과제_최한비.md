# Seq2Seq 불규칙파 예측 실험 보고서

## 실험 환경
- Google Colab T4 GPU, PyTorch 2.10, Optuna 4.8

---

## AI 활용 내역
- 모델 기본 구조(Encoder-Decoder, Teacher Forcing) 코드 스캐폴딩에 AI 활용
- Optuna objective 함수 템플릿 생성에 AI 활용
- 실험 설계(탐색 범위, 비교 조건 결정)와 결과 해석은 직접 수행

---

## Step 1 - waves별 RNN 한계
waves=1,2는 MSE 0.006~0.007 수준으로 어느 정도 학습이 됐는데, waves=3에서 0.0205로 급등하며 학습 실패가 확인됐다. Scatter plot도 예측값이 한쪽으로 몰리는 현상이 나타났다. hidden=32짜리 RNN으로는 세 개의 주파수가 섞인 신호를 표현하기 역부족인 것으로 보인다.

## Step 2 - Optuna HPO
hidden_size=128, lr=0.000430, Adam 조합이 최적으로 선택됐고, 베이스라인 대비 MSE가 0.0128에서 0.0033으로 74.4% 감소했다. hidden을 키운 게 가장 효과적이었던 것 같다.

## Step 3 - 시퀀스 길이
input_len을 30→60→100으로 늘릴수록 MSE가 0.0038→0.0024→0.0014로 개선됐다. 다만 100에서는 loss 곡선이 불안정해지는 게 보였다. 정보가 많아지면 좋긴 한데 RNN 특성상 긴 시퀀스 초반 정보는 잘 못 쓰는 한계가 있다.

## Step 4 - RNN vs LSTM vs GRU
RNN(0.0044) < GRU(0.0021) < LSTM(0.0019) 순으로 LSTM이 가장 좋았다. 예상은 GRU가 더 좋을 것 같았는데 이 데이터에서는 LSTM의 cell state가 더 효과적으로 작동한 것 같다. 게이트 구조 도입만으로 RNN 대비 50% 이상 개선됐다.

## Step 5 - Teacher Forcing Ratio
TF=1.0(0.0030) < TF=0.5(0.0013) < TF=0.0(0.0008) 순으로 낮을수록 성능이 좋았다. TF=1.0은 훈련할 때는 안정적이지만 실제 추론 환경과 달라서 오차가 누적되는 문제가 있었다. TF=0.5가 생각보다 잘 나온 게 흥미로웠다.

---

## 결론
아키텍처 변경(RNN→LSTM)이 HPO보다 성능 개선에 더 직접적인 영향을 줬다. Teacher Forcing은 낮을수록 실제 추론 성능이 좋으나 학습 안정성을 위해 0.5 정도가 실용적인 선택으로 보인다.
